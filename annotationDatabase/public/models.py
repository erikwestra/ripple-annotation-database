""" annotationDatabase.public.models

    This file contains the Django models used by the Annotation Database's
    "public" application.  Note that this module is deliberately empty, as the
    annotationDatase.public application doesn't define any database models of
    its own.
"""
# Module left deliberately empty.
