""" boxUI.models

    This file contains the Django models used by the "boxUI" application.

    Note that this module is deliberately left empty, as the "boxUI"
    application doesn't define any database models of its own.
"""
# This module is deliberately left empty.
