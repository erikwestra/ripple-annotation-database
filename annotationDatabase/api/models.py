""" annotationDatabase.api.models

    This file contains the Django models used by the "annotationDatabase.api"
    application.

    Note that this module is deliberately left empty, as this application
    doesn't define any database models of its own.
"""
# This module is deliberately left empty.
